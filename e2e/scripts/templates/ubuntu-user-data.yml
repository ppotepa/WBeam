# Template reference for Ubuntu autoinstall base images.
# The active seed is generated by e2e/scripts/seed.py.
#
# Required behavior:
# - Subiquity autoinstall
# - create user wbeam
# - inject SSH key
# - install openssh-server and sudo
# - install GNOME packages for desktop sessions
# - write /var/lib/wbeam-e2e/base-ready.json
