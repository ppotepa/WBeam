[package]
name = "wbeam-trainer-tauri-shell"
version = "0.1.0"
edition = "2021"
description = "WBeam trainer desktop app shell (Tauri + Solid + TypeScript)"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
